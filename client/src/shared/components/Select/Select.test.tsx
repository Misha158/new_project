describe("<Select />", () => {
  it("Should show loading when typing", () => {});
  it("Should call fetch function after enter the text", () => {});
  it("Should show selected option", () => {});
  it("Should show dropdown options", () => {});
});
