describe("test", () => {
  it("Should open modal - and should close modal", () => {
    // use rerender from render
  });
  it("Should call on next handler", () => {});
  it("Should call on back handler", () => {});
  it("Should submit form", () => {});
});
